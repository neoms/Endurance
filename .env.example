# Server
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database (relative to prisma/ directory)
DATABASE_URL="file:./dev.db"

# Auth
JWT_SECRET=dev-only-secret-do-not-use-in-production
JWT_EXPIRES_IN=24h
